version: '3.8'
name: sql_test

services:
  db:
    container_name: mysql8_4
    image: mysql:8.4
#    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3388:3306"
    volumes:
      - mysql_data_test:/var/lib/mysql

volumes:
      mysql_data_test: